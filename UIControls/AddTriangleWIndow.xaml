<Window x:Class="TempoEngine.UIControls.AddTriangleWIndow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TempoEngine.UIControls"
        mc:Ignorable="d"
        Title="AddTriangleWIndow" Height="450" Width="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!-- For top buttons panel -->
            <RowDefinition Height="30"/>
            <!-- For content area -->
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Add" Click="AddButton_Click"/>
            <Button Grid.Column="1" Content="Cancel" Click="CancelButton_Click"/>
        </Grid>
    </Grid>
</Window>
